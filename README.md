# dessinPlot

A very quick and dirty implementation of plotting for the dessin d'enfant of a rational function in sage.

This assumes that the variable of the function f is 'z', pass this function to `drawDessin()` and the output is a matplotlib view of the corresponding dessin denfant
